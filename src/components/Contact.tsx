import { Container, Typography } from '@mui/material';

const Contact = () => {
    return (
        <Container>
            <Typography variant="h3">Hello this is the Contact page</Typography>
        </Container>
    );
};

export default Contact;
